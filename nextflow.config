process {
    clusterOptions = '--account=vh83'
    errorStrategy = 'retry'
    maxRetries = 5
    memory = { task.memory * (1.5 * task.attempt) }
    time = { task.time * (1.5 * task.attempt) }
}

executor {
    name = 'slurm'
    queueSize = 200
    pollInterval = '30 sec'
}
